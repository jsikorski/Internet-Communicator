<Window x:Class="Client.Features.Ragistration.RegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RegisterView"
        Width="300"
        Height="260"
        WindowStartupLocation="CenterScreen">

    <StackPanel Orientation="Vertical"
                Margin="10">
        <Label Content="Password" />
        <TextBox Text="{Binding Password, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Content="Confirm password" />
        <TextBox Text="{Binding PasswordConfirmation, Mode=OneWayToSource, UpdateSourceTrigger=PropertyChanged}"/>

        <Button Name="Register"
                Content="Register"
                Height="40"
                Margin="0, 10, 0, 0" />
        <Button Name="Return"
                Content="Return"
                Height="40"
                Margin="0, 10, 0, 0" />
    </StackPanel>
</Window>
